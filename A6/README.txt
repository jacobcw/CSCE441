ASSIGNMENT 6 - JACOB WALLACE
- My MoCap files are under asf_amc_viewer/viewer/MoCap
- Buttons are self explainatory for function
		- View will cycle through viewing the keyframes
		- Need atleast 4 keyframes to interpolate a motion
- You CAN load another motion after you interpolate one and it will reset
  keyframes and interpolated motion
- When you press interpolate it will load that motion into the viewer